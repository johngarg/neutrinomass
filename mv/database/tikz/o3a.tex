\documentclass{standalone}
\usepackage{tikz}
\usepackage{tikz-feynman}

\begin{document}
\feynmandiagram [spring layout, vertical=a to b] {
  a [particle=$H$] -- [charged scalar] b -- [fermion, edge label'=$\bar{d}$, out=180, looseness=1.4] c
  [blob] -- [anti fermion, edge label'=$Q$, in=0, looseness=1.4] b,
  d [particle=$L$] -- [fermion] c,
  g [particle=$L$] -- [fermion] e -- [edge label=$L$, fermion] c,
  f [particle=$H$] -- [charged scalar] h -- [edge label'=$H$, charged scalar] c,
  e -- [edge label'=$W$, photon] h,
};

\end{document}
